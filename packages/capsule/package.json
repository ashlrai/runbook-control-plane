{
  "name": "@runbook/capsule",
  "version": "0.1.0",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "pretest": "pnpm --filter @runbook/engine build",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@runbook/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.19.8",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
